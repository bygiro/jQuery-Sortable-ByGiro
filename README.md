# jQuery plugin: Sortable ByGiro

A jQuery plugin to drag'n'drop and sort elements within a container or multiple nested containers.


### Install with Bower

```bash
$ bower install jquery-sortableByGiro --save
```

check the demo

(documentation coming soon!)
